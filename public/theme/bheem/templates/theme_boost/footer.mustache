<!-- [Edoo] Footer Component - Enhanced Modern Design -->
<style>
    /* Premium Footer - Ultra-Modern Design with Enhanced Gradients */
    .premium-footer-optimized {
        position: relative;
        background:
            linear-gradient(135deg, #0F172A 0%, #1E293B 25%, #312E81 50%, #1E293B 75%, #0F172A 100%);
        color: #E2E8F0;
        overflow: hidden;
        margin-top: 40px;
    }

    /* Enhanced Aurora Background Effect with Multiple Layers */
    .aurora-bg {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 1;
        overflow: hidden;
    }

    .aurora-layer {
        position: absolute;
        width: 250%;
        height: 250%;
        filter: blur(100px);
    }

    .aurora-1 {
        top: -60%;
        left: -60%;
        background: radial-gradient(ellipse at center,
            rgba(139, 92, 246, 0.5) 0%,
            rgba(168, 85, 247, 0.3) 30%,
            transparent 70%);
        animation: aurora-drift-1 25s ease-in-out infinite;
        opacity: 0.3;
    }

    .aurora-2 {
        bottom: -60%;
        right: -60%;
        background: radial-gradient(ellipse at center,
            rgba(6, 182, 212, 0.5) 0%,
            rgba(14, 165, 233, 0.3) 30%,
            transparent 70%);
        animation: aurora-drift-2 30s ease-in-out infinite;
        opacity: 0.3;
    }

    .aurora-3 {
        top: 20%;
        right: 20%;
        width: 200%;
        height: 200%;
        background: radial-gradient(ellipse at center,
            rgba(236, 72, 153, 0.4) 0%,
            rgba(244, 114, 182, 0.2) 30%,
            transparent 70%);
        animation: aurora-drift-3 35s ease-in-out infinite;
        opacity: 0.25;
        filter: blur(120px);
    }

    @keyframes aurora-drift-1 {
        0%, 100% { transform: translate(0, 0) rotate(0deg) scale(1); }
        33% { transform: translate(15%, -8%) rotate(8deg) scale(1.1); }
        66% { transform: translate(-8%, 12%) rotate(-5deg) scale(0.95); }
    }

    @keyframes aurora-drift-2 {
        0%, 100% { transform: translate(0, 0) rotate(0deg) scale(1); }
        33% { transform: translate(-12%, 8%) rotate(-6deg) scale(1.05); }
        66% { transform: translate(8%, -15%) rotate(8deg) scale(0.9); }
    }

    @keyframes aurora-drift-3 {
        0%, 100% { transform: translate(0, 0) rotate(0deg) scale(1); }
        50% { transform: translate(-10%, 10%) rotate(10deg) scale(1.15); }
    }

    /* Enhanced Floating AI Tech Elements with Gradient Backgrounds */
    .ai-tech-elements {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        pointer-events: none;
        z-index: 2;
    }

    .tech-bubble {
        position: absolute;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 6px;
        padding: 14px 20px;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.2),
            rgba(236, 72, 153, 0.15),
            rgba(6, 182, 212, 0.2));
        backdrop-filter: blur(15px);
        border: 2px solid transparent;
        border-image: linear-gradient(135deg,
            rgba(139, 92, 246, 0.5),
            rgba(6, 182, 212, 0.5)) 1;
        border-radius: 20px;
        font-size: 12px;
        color: rgba(255, 255, 255, 0.9);
        animation: tech-float 15s ease-in-out infinite;
        box-shadow:
            0 8px 32px rgba(139, 92, 246, 0.3),
            inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }

    .tech-bubble[data-tech="nn"] {
        top: 10%;
        left: 5%;
        animation: tech-float-1 18s ease-in-out infinite;
        animation-delay: 0s;
        background: linear-gradient(135deg, rgba(139, 92, 246, 0.25), rgba(168, 85, 247, 0.15));
    }
    .tech-bubble[data-tech="llama"] {
        top: 25%;
        right: 8%;
        animation: tech-float-2 22s ease-in-out infinite;
        animation-delay: 2s;
        background: linear-gradient(135deg, rgba(236, 72, 153, 0.25), rgba(244, 114, 182, 0.15));
    }
    .tech-bubble[data-tech="gpt"] {
        top: 50%;
        left: 3%;
        animation: tech-float-3 20s ease-in-out infinite;
        animation-delay: 4s;
        background: linear-gradient(135deg, rgba(6, 182, 212, 0.25), rgba(14, 165, 233, 0.15));
    }
    .tech-bubble[data-tech="nocode"] {
        top: 65%;
        right: 5%;
        animation: tech-float-4 24s ease-in-out infinite;
        animation-delay: 6s;
        background: linear-gradient(135deg, rgba(16, 185, 129, 0.25), rgba(52, 211, 153, 0.15));
    }
    .tech-bubble[data-tech="finetune"] {
        bottom: 20%;
        left: 7%;
        animation: tech-float-5 19s ease-in-out infinite;
        animation-delay: 8s;
        background: linear-gradient(135deg, rgba(245, 158, 11, 0.25), rgba(251, 191, 36, 0.15));
    }
    .tech-bubble[data-tech="ml"] {
        bottom: 35%;
        right: 10%;
        animation: tech-float-6 21s ease-in-out infinite;
        animation-delay: 10s;
        background: linear-gradient(135deg, rgba(239, 68, 68, 0.25), rgba(248, 113, 113, 0.15));
    }

    /* Individual floating animations for each tech bubble */
    @keyframes tech-float-1 {
        0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
        25% { transform: translate(30px, -25px) scale(1.08) rotate(5deg); opacity: 1; }
        50% { transform: translate(-20px, -40px) scale(1.05) rotate(-3deg); opacity: 0.9; }
        75% { transform: translate(15px, -15px) scale(1.1) rotate(2deg); opacity: 0.95; }
    }

    @keyframes tech-float-2 {
        0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
        30% { transform: translate(-35px, 30px) scale(1.1) rotate(-6deg); opacity: 1; }
        60% { transform: translate(25px, -35px) scale(1.06) rotate(4deg); opacity: 0.9; }
        80% { transform: translate(-15px, 20px) scale(1.08) rotate(-2deg); opacity: 1; }
    }

    @keyframes tech-float-3 {
        0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
        25% { transform: translate(40px, 30px) scale(1.12) rotate(7deg); opacity: 1; }
        50% { transform: translate(-25px, -30px) scale(1.04) rotate(-5deg); opacity: 0.9; }
        75% { transform: translate(20px, 25px) scale(1.09) rotate(3deg); opacity: 0.95; }
    }

    @keyframes tech-float-4 {
        0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
        20% { transform: translate(-30px, -40px) scale(1.07) rotate(-4deg); opacity: 1; }
        50% { transform: translate(35px, 35px) scale(1.11) rotate(6deg); opacity: 0.9; }
        75% { transform: translate(-20px, 15px) scale(1.05) rotate(-3deg); opacity: 1; }
    }

    @keyframes tech-float-5 {
        0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
        30% { transform: translate(25px, -35px) scale(1.09) rotate(5deg); opacity: 1; }
        55% { transform: translate(-30px, 25px) scale(1.06) rotate(-4deg); opacity: 0.9; }
        85% { transform: translate(18px, -20px) scale(1.1) rotate(3deg); opacity: 0.95; }
    }

    @keyframes tech-float-6 {
        0%, 100% { transform: translate(0, 0) scale(1) rotate(0deg); opacity: 0.8; }
        25% { transform: translate(-25px, 30px) scale(1.08) rotate(-6deg); opacity: 1; }
        50% { transform: translate(30px, -25px) scale(1.12) rotate(4deg); opacity: 0.9; }
        75% { transform: translate(-15px, -30px) scale(1.05) rotate(-2deg); opacity: 1; }
    }

    .tech-icon {
        font-size: 22px;
        font-weight: 700;
        background: linear-gradient(135deg, #FFFFFF, #E0E7FF);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .tech-name {
        font-size: 11px;
        font-weight: 700;
        text-transform: uppercase;
        letter-spacing: 1px;
        text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    }

    /* Enhanced Live Activity Bar with Gradient Background */
    .activity-bar {
        position: relative;
        z-index: 3;
        background: linear-gradient(90deg,
            rgba(139, 92, 246, 0.25) 0%,
            rgba(236, 72, 153, 0.2) 25%,
            rgba(6, 182, 212, 0.25) 50%,
            rgba(16, 185, 129, 0.2) 75%,
            rgba(139, 92, 246, 0.25) 100%);
        background-size: 200% 100%;
        animation: gradient-shift 8s ease-in-out infinite;
        border-bottom: 2px solid rgba(139, 92, 246, 0.3);
        border-top: 2px solid rgba(139, 92, 246, 0.2);
        padding: 18px 0;
        overflow: hidden;
        box-shadow:
            0 4px 20px rgba(139, 92, 246, 0.2),
            inset 0 1px 0 rgba(255, 255, 255, 0.1);
    }

    /* Smooth fade edges for professional scroll effect */
    .activity-bar::before,
    .activity-bar::after {
        content: '';
        position: absolute;
        top: 0;
        bottom: 0;
        width: 120px;
        z-index: 2;
        pointer-events: none;
    }

    .activity-bar::before {
        left: 0;
        background: linear-gradient(90deg,
            rgba(15, 23, 42, 1) 0%,
            rgba(15, 23, 42, 0.8) 30%,
            transparent 100%);
    }

    .activity-bar::after {
        right: 0;
        background: linear-gradient(270deg,
            rgba(15, 23, 42, 1) 0%,
            rgba(15, 23, 42, 0.8) 30%,
            transparent 100%);
    }

    @keyframes gradient-shift {
        0%, 100% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
    }

    .activity-wrapper {
        display: flex;
        gap: 64px;
        animation: activity-scroll 30s linear infinite;
        will-change: transform;
    }

    /* Pause animation on hover for readability */
    .activity-bar:hover .activity-wrapper {
        animation-play-state: paused;
    }

    @keyframes activity-scroll {
        0% { transform: translateX(0); }
        100% { transform: translateX(-50%); }
    }

    .activity-item {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        white-space: nowrap;
        font-size: 15px;
        font-weight: 600;
        color: #FFFFFF;
        text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
        padding: 10px 20px;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.25),
            rgba(236, 72, 153, 0.2),
            rgba(6, 182, 212, 0.25));
        border-radius: 100px;
        border: 1px solid rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        box-shadow:
            0 4px 12px rgba(139, 92, 246, 0.3),
            inset 0 1px 0 rgba(255, 255, 255, 0.2);
        transition: all 0.3s ease;
    }

    .activity-item:hover {
        transform: translateY(-2px);
        box-shadow:
            0 6px 20px rgba(139, 92, 246, 0.5),
            inset 0 1px 0 rgba(255, 255, 255, 0.3);
        border-color: rgba(139, 92, 246, 0.6);
    }

    .activity-item i {
        font-size: 16px;
        background: linear-gradient(135deg, #A78BFA, #EC4899, #22D3EE);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        filter: drop-shadow(0 0 8px rgba(139, 92, 246, 0.6));
    }

    .activity-item .pulse {
        animation: pulse-dot 2s ease-in-out infinite;
        -webkit-text-fill-color: #10B981;
        filter: drop-shadow(0 0 12px rgba(16, 185, 129, 0.8));
    }

    @keyframes pulse-dot {
        0%, 100% {
            opacity: 1;
            transform: scale(1);
            filter: drop-shadow(0 0 8px rgba(16, 185, 129, 0.8));
        }
        50% {
            opacity: 0.6;
            transform: scale(1.3);
            filter: drop-shadow(0 0 16px rgba(16, 185, 129, 1));
        }
    }

    .activity-item strong {
        background: linear-gradient(135deg, #A78BFA, #F472B6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        font-weight: 800;
        letter-spacing: 0.5px;
    }

    /* Enhanced Statistics Bar with Gradient Cards */
    .stats-bar-compact {
        position: relative;
        z-index: 3;
        background: linear-gradient(135deg,
            rgba(15, 23, 42, 0.9),
            rgba(30, 41, 59, 0.9),
            rgba(49, 46, 129, 0.8));
        backdrop-filter: blur(20px);
        padding: 64px 24px;
        border-bottom: 2px solid rgba(139, 92, 246, 0.3);
    }

    .stats-grid-compact {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: 32px;
        max-width: 1400px;
        margin: 0 auto;
    }

    .stat-item-compact {
        display: flex;
        align-items: center;
        gap: 20px;
        padding: 32px;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.15),
            rgba(236, 72, 153, 0.1),
            rgba(6, 182, 212, 0.15));
        border: 2px solid transparent;
        border-image: linear-gradient(135deg,
            rgba(139, 92, 246, 0.5),
            rgba(236, 72, 153, 0.3),
            rgba(6, 182, 212, 0.5)) 1;
        border-radius: 20px;
        transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
        position: relative;
        overflow: hidden;
    }

    .stat-item-compact::before {
        content: '';
        position: absolute;
        top: 0;
        left: -100%;
        width: 100%;
        height: 100%;
        background: linear-gradient(90deg,
            transparent,
            rgba(255, 255, 255, 0.1),
            transparent);
        transition: left 0.6s ease;
    }

    .stat-item-compact:hover::before {
        left: 100%;
    }

    .stat-item-compact:hover {
        transform: translateY(-8px) scale(1.02);
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.25),
            rgba(236, 72, 153, 0.15),
            rgba(6, 182, 212, 0.25));
        border-image: linear-gradient(135deg,
            rgba(139, 92, 246, 0.8),
            rgba(236, 72, 153, 0.5),
            rgba(6, 182, 212, 0.8)) 1;
        box-shadow:
            0 20px 60px rgba(139, 92, 246, 0.4),
            0 10px 30px rgba(236, 72, 153, 0.3),
            inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }

    .stat-item-compact i {
        font-size: 40px;
        background: linear-gradient(135deg, #8B5CF6, #EC4899, #06B6D4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        transition: all 0.4s ease;
        filter: drop-shadow(0 4px 8px rgba(139, 92, 246, 0.5));
    }

    .stat-item-compact:hover i {
        transform: scale(1.3) rotate(10deg);
        filter: drop-shadow(0 6px 12px rgba(139, 92, 246, 0.7));
    }

    .stat-item-compact h3 {
        font-size: 3rem;
        font-weight: 900;
        background: linear-gradient(135deg, #FFFFFF, #E0E7FF, #C7D2FE);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin: 0;
        line-height: 1;
        text-shadow: 0 4px 20px rgba(139, 92, 246, 0.5);
    }

    .stat-item-compact .stat-suffix {
        font-size: 2rem;
        font-weight: 800;
        background: linear-gradient(135deg, #8B5CF6, #A78BFA);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-left: 6px;
    }

    .stat-item-compact p {
        font-size: 15px;
        color: #C7D2FE;
        margin: 6px 0 0 0;
        font-weight: 600;
        letter-spacing: 0.5px;
    }

    /* Enhanced Main Footer Content */
    .footer-main-optimized {
        position: relative;
        z-index: 3;
        padding: 80px 24px 64px 24px;
        background: linear-gradient(180deg,
            rgba(15, 23, 42, 0.5) 0%,
            rgba(30, 41, 59, 0.3) 100%);
    }

    .footer-main-optimized .container {
        max-width: 1400px;
        margin: 0 auto;
    }

    .footer-grid-optimized {
        display: grid;
        grid-template-columns: 1.2fr 1fr 1fr 1.3fr;
        gap: 56px;
        margin-bottom: 56px;
    }

    /* Enhanced Brand Column */
    .brand-column-opt .footer-logo img {
        height: 55px;
        margin-bottom: 24px;
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        filter: drop-shadow(0 4px 12px rgba(139, 92, 246, 0.4));
    }

    .brand-column-opt .footer-logo img:hover {
        transform: scale(1.08) translateY(-4px);
        filter: drop-shadow(0 8px 20px rgba(139, 92, 246, 0.6));
    }

    .brand-desc {
        font-size: 16px;
        line-height: 1.8;
        color: #C7D2FE;
        margin-bottom: 28px;
        font-weight: 500;
    }

    .social-links-opt {
        display: flex;
        gap: 14px;
    }

    .social-link-opt {
        width: 50px;
        height: 50px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.2),
            rgba(236, 72, 153, 0.15));
        border: 2px solid rgba(139, 92, 246, 0.3);
        border-radius: 14px;
        color: #E2E8F0;
        font-size: 20px;
        transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
        text-decoration: none;
        position: relative;
        overflow: hidden;
    }

    .social-link-opt::before {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        width: 0;
        height: 0;
        background: linear-gradient(135deg, #8B5CF6, #EC4899, #06B6D4);
        border-radius: 50%;
        transform: translate(-50%, -50%);
        transition: width 0.5s ease, height 0.5s ease;
        z-index: -1;
    }

    .social-link-opt:hover::before {
        width: 200%;
        height: 200%;
    }

    .social-link-opt:hover {
        transform: translateY(-6px) scale(1.15) rotate(5deg);
        border-color: transparent;
        box-shadow:
            0 16px 40px rgba(139, 92, 246, 0.5),
            0 8px 20px rgba(236, 72, 153, 0.4);
        color: #FFFFFF;
    }

    /* Enhanced Links Columns */
    .column-title {
        font-family: 'Poppins', sans-serif;
        font-size: 20px;
        font-weight: 800;
        background: linear-gradient(135deg, #FFFFFF, #E0E7FF);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 24px;
        display: flex;
        align-items: center;
        gap: 12px;
        letter-spacing: 0.5px;
    }

    .column-title i {
        font-size: 24px;
        background: linear-gradient(135deg, #8B5CF6, #EC4899);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .links-grid {
        display: flex;
        flex-direction: column;
        gap: 14px;
    }

    .footer-link-opt {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 15px;
        color: #C7D2FE;
        text-decoration: none;
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        padding: 8px 12px;
        border-radius: 10px;
        font-weight: 600;
    }

    .footer-link-opt i {
        color: #8B5CF6;
        font-size: 13px;
        transition: all 0.4s ease;
    }

    .footer-link-opt:hover {
        color: #FFFFFF;
        padding-left: 16px;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.2),
            rgba(236, 72, 153, 0.1));
        transform: translateX(4px);
    }

    .footer-link-opt:hover i {
        transform: translateX(6px) scale(1.2);
        color: #EC4899;
    }

    /* Enhanced Newsletter Column */
    .newsletter-box-opt {
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.15),
            rgba(236, 72, 153, 0.1),
            rgba(6, 182, 212, 0.15));
        border: 2px solid rgba(139, 92, 246, 0.3);
        border-radius: 20px;
        padding: 28px;
        margin-bottom: 28px;
        position: relative;
        overflow: hidden;
    }

    .newsletter-box-opt::before {
        content: '';
        position: absolute;
        top: -50%;
        right: -50%;
        width: 200%;
        height: 200%;
        background: radial-gradient(circle,
            rgba(139, 92, 246, 0.1) 0%,
            transparent 70%);
        animation: newsletter-glow 8s ease-in-out infinite;
    }

    @keyframes newsletter-glow {
        0%, 100% { transform: translate(0, 0) scale(1); opacity: 0.5; }
        50% { transform: translate(-10%, 10%) scale(1.2); opacity: 0.8; }
    }

    .newsletter-text {
        font-size: 15px;
        color: #C7D2FE;
        margin-bottom: 20px;
        font-weight: 600;
        position: relative;
        z-index: 1;
    }

    .newsletter-form-opt {
        display: flex;
        gap: 10px;
        margin-bottom: 20px;
        position: relative;
        z-index: 1;
    }

    .newsletter-form-opt input {
        flex: 1;
        padding: 14px 18px;
        background: rgba(15, 23, 42, 0.9);
        border: 2px solid rgba(139, 92, 246, 0.3);
        border-radius: 12px;
        color: #FFFFFF;
        font-size: 15px;
        font-weight: 600;
        transition: all 0.4s ease;
    }

    .newsletter-form-opt input:focus {
        outline: none;
        border-color: #8B5CF6;
        background: rgba(15, 23, 42, 1);
        box-shadow:
            0 0 0 4px rgba(139, 92, 246, 0.2),
            0 8px 24px rgba(139, 92, 246, 0.3);
        transform: translateY(-2px);
    }

    .newsletter-form-opt input::placeholder {
        color: #94A3B8;
        font-weight: 500;
    }

    .newsletter-form-opt button {
        display: flex;
        align-items: center;
        gap: 10px;
        padding: 14px 24px;
        background: linear-gradient(135deg, #8B5CF6, #EC4899, #06B6D4);
        background-size: 200% auto;
        border: none;
        border-radius: 12px;
        color: #FFFFFF;
        font-size: 15px;
        font-weight: 700;
        cursor: pointer;
        transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
        box-shadow: 0 8px 24px rgba(139, 92, 246, 0.4);
    }

    .newsletter-form-opt button:hover {
        transform: translateY(-4px) scale(1.05);
        box-shadow:
            0 16px 40px rgba(139, 92, 246, 0.5),
            0 8px 20px rgba(236, 72, 153, 0.4);
        background-position: right center;
    }

    .newsletter-form-opt button i {
        transition: transform 0.4s ease;
        font-size: 16px;
    }

    .newsletter-form-opt button:hover i {
        transform: translateX(6px);
    }

    .newsletter-features {
        display: flex;
        flex-direction: column;
        gap: 10px;
        position: relative;
        z-index: 1;
    }

    .newsletter-features span {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: 13px;
        color: #C7D2FE;
        font-weight: 600;
    }

    .newsletter-features i {
        color: #10B981;
        font-size: 16px;
        filter: drop-shadow(0 2px 4px rgba(16, 185, 129, 0.5));
    }

    /* Enhanced Contact Info */
    .contact-info-opt {
        display: flex;
        flex-direction: column;
        gap: 14px;
    }

    .contact-item-opt {
        display: flex;
        align-items: center;
        gap: 14px;
        padding: 14px 16px;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.1),
            rgba(6, 182, 212, 0.05));
        border-radius: 12px;
        font-size: 15px;
        color: #C7D2FE;
        font-weight: 600;
        border: 1px solid rgba(139, 92, 246, 0.2);
        transition: all 0.3s ease;
    }

    .contact-item-opt:hover {
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.2),
            rgba(6, 182, 212, 0.1));
        border-color: rgba(139, 92, 246, 0.4);
        transform: translateX(4px);
    }

    .contact-item-opt i {
        font-size: 18px;
        background: linear-gradient(135deg, #8B5CF6, #06B6D4);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    /* Enhanced Featured Courses Bar */
    .featured-courses-bar {
        padding: 40px 0;
        border-top: 2px solid rgba(139, 92, 246, 0.3);
        border-bottom: 2px solid rgba(139, 92, 246, 0.3);
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.05),
            rgba(236, 72, 153, 0.03),
            rgba(6, 182, 212, 0.05));
    }

    .featured-courses-bar h4 {
        font-size: 18px;
        font-weight: 800;
        background: linear-gradient(135deg, #FFFFFF, #E0E7FF);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
        margin-bottom: 20px;
        display: flex;
        align-items: center;
        gap: 12px;
        letter-spacing: 0.5px;
    }

    .featured-courses-bar h4 i {
        color: #F59E0B;
        animation: fire-flicker 2s ease-in-out infinite;
        filter: drop-shadow(0 2px 8px rgba(245, 158, 11, 0.6));
        font-size: 20px;
    }

    @keyframes fire-flicker {
        0%, 100% { opacity: 1; transform: scale(1); }
        50% { opacity: 0.7; transform: scale(1.15); }
    }

    .courses-slider {
        display: flex;
        gap: 14px;
        overflow-x: auto;
        padding-bottom: 10px;
        scrollbar-width: thin;
        scrollbar-color: rgba(139, 92, 246, 0.5) rgba(255, 255, 255, 0.05);
    }

    .courses-slider::-webkit-scrollbar {
        height: 8px;
    }

    .courses-slider::-webkit-scrollbar-track {
        background: rgba(255, 255, 255, 0.05);
        border-radius: 4px;
    }

    .courses-slider::-webkit-scrollbar-thumb {
        background: linear-gradient(90deg, #8B5CF6, #EC4899);
        border-radius: 4px;
    }

    .course-chip {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        padding: 14px 24px;
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.15),
            rgba(236, 72, 153, 0.1),
            rgba(6, 182, 212, 0.15));
        border: 2px solid rgba(139, 92, 246, 0.3);
        border-radius: 100px;
        color: #E2E8F0;
        font-size: 15px;
        font-weight: 700;
        text-decoration: none;
        white-space: nowrap;
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
    }

    .course-chip i {
        font-size: 18px;
        background: linear-gradient(135deg, #8B5CF6, #EC4899);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
    }

    .course-chip:hover {
        background: linear-gradient(135deg,
            rgba(139, 92, 246, 0.3),
            rgba(236, 72, 153, 0.2),
            rgba(6, 182, 212, 0.3));
        border-color: #8B5CF6;
        transform: translateY(-4px) scale(1.05);
        box-shadow: 0 12px 32px rgba(139, 92, 246, 0.4);
        color: #FFFFFF;
    }

    /* Enhanced Footer Bottom */
    .footer-bottom-opt {
        position: relative;
        z-index: 3;
        background: linear-gradient(135deg,
            rgba(15, 23, 42, 0.95),
            rgba(30, 41, 59, 0.9));
        border-top: 2px solid rgba(139, 92, 246, 0.3);
        padding: 36px 24px;
        backdrop-filter: blur(10px);
    }

    .bottom-content-opt {
        max-width: 1400px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        align-items: center;
        flex-wrap: wrap;
        gap: 28px;
    }

    .copyright-opt p {
        font-size: 15px;
        color: #C7D2FE;
        margin: 0;
        font-weight: 600;
    }

    .legal-links-opt {
        display: flex;
        gap: 28px;
    }

    .legal-links-opt a {
        font-size: 15px;
        color: #C7D2FE;
        text-decoration: none;
        transition: all 0.3s ease;
        font-weight: 600;
        position: relative;
    }

    .legal-links-opt a::after {
        content: '';
        position: absolute;
        bottom: -4px;
        left: 0;
        width: 0;
        height: 2px;
        background: linear-gradient(90deg, #8B5CF6, #EC4899);
        transition: width 0.3s ease;
    }

    .legal-links-opt a:hover {
        color: #FFFFFF;
    }

    .legal-links-opt a:hover::after {
        width: 100%;
    }

    .payment-badges {
        display: flex;
        gap: 16px;
        align-items: center;
    }

    .payment-badges i {
        font-size: 32px;
        color: #64748B;
        transition: all 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
        filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.3));
    }

    .payment-badges i:hover {
        color: #8B5CF6;
        transform: scale(1.2) translateY(-4px);
        filter: drop-shadow(0 4px 12px rgba(139, 92, 246, 0.6));
    }

    /* Enhanced Floating Chat Widget */
    .chat-widget {
        position: fixed;
        bottom: 36px;
        right: 36px;
        z-index: 9999;
    }

    .chat-trigger {
        width: 68px;
        height: 68px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #8B5CF6, #EC4899, #06B6D4);
        background-size: 200% auto;
        border: none;
        border-radius: 50%;
        color: #FFFFFF;
        font-size: 28px;
        cursor: pointer;
        box-shadow:
            0 20px 50px rgba(139, 92, 246, 0.5),
            0 10px 25px rgba(236, 72, 153, 0.4);
        transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
        position: relative;
        animation: chat-pulse 3s ease-in-out infinite;
    }

    @keyframes chat-pulse {
        0%, 100% { box-shadow: 0 20px 50px rgba(139, 92, 246, 0.5), 0 10px 25px rgba(236, 72, 153, 0.4); }
        50% { box-shadow: 0 25px 60px rgba(139, 92, 246, 0.7), 0 15px 35px rgba(236, 72, 153, 0.6); }
    }

    .chat-trigger:hover {
        transform: scale(1.15) translateY(-6px) rotate(5deg);
        background-position: right center;
        box-shadow:
            0 28px 70px rgba(139, 92, 246, 0.7),
            0 14px 35px rgba(236, 72, 153, 0.6);
    }

    .chat-badge {
        position: absolute;
        top: -6px;
        right: -6px;
        width: 28px;
        height: 28px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: linear-gradient(135deg, #EF4444, #DC2626);
        border: 3px solid #0F172A;
        border-radius: 50%;
        font-size: 13px;
        font-weight: 800;
        animation: badge-pulse 2s ease-in-out infinite;
        box-shadow: 0 4px 12px rgba(239, 68, 68, 0.6);
    }

    @keyframes badge-pulse {
        0%, 100% { transform: scale(1); }
        50% { transform: scale(1.25); }
    }

    /* Responsive Design */
    @media (max-width: 1024px) {
        .footer-grid-optimized {
            grid-template-columns: 1fr 1fr;
            gap: 48px;
        }

        .stats-grid-compact {
            grid-template-columns: repeat(2, 1fr);
            gap: 24px;
        }
    }

    @media (max-width: 768px) {
        .footer-grid-optimized {
            grid-template-columns: 1fr;
            gap: 40px;
        }

        .stats-grid-compact {
            grid-template-columns: 1fr;
            gap: 20px;
        }

        .bottom-content-opt {
            flex-direction: column;
            text-align: center;
        }

        .legal-links-opt {
            flex-wrap: wrap;
            justify-content: center;
        }

        .newsletter-form-opt {
            flex-direction: column;
        }

        .newsletter-form-opt button {
            width: 100%;
            justify-content: center;
        }

        .activity-wrapper {
            gap: 32px;
        }

        .activity-item {
            font-size: 14px;
            padding: 6px 12px;
        }

        .chat-widget {
            bottom: 24px;
            right: 24px;
        }

        .chat-trigger {
            width: 60px;
            height: 60px;
            font-size: 24px;
        }

        .stat-item-compact h3 {
            font-size: 2.5rem;
        }
    }

    @media (max-width: 480px) {
        .premium-footer-optimized {
            margin-top: 30px;
        }

        .footer-main-optimized {
            padding: 56px 16px 40px 16px;
        }

        .stats-bar-compact {
            padding: 40px 16px;
        }

        .footer-bottom-opt {
            padding: 28px 16px;
        }

        .brand-column-opt .footer-logo img {
            height: 45px;
        }

        .column-title {
            font-size: 18px;
        }

        .stat-item-compact h3 {
            font-size: 2rem;
        }

        .stat-item-compact {
            padding: 24px;
        }
    }

    /* Accessibility */
    @media (prefers-reduced-motion: reduce) {
        .premium-footer-optimized *,
        .premium-footer-optimized *::before,
        .premium-footer-optimized *::after {
            animation-duration: 0.01ms !important;
            animation-iteration-count: 1 !important;
            transition-duration: 0.01ms !important;
        }
    }
</style>

<!-- Footer HTML -->
<footer class="premium-footer-optimized" id="optimizedFooter">
    <!-- Enhanced Aurora Background Effect -->
    <div class="aurora-bg">
        <div class="aurora-layer aurora-1"></div>
        <div class="aurora-layer aurora-2"></div>
        <div class="aurora-layer aurora-3"></div>
    </div>

    <!-- Floating AI Tech Elements -->
    <div class="ai-tech-elements">
        <div class="tech-bubble" data-tech="nn">
            <span class="tech-icon">NN</span>
            <span class="tech-name">Neural Net</span>
        </div>
        <div class="tech-bubble" data-tech="llama">
            <span class="tech-icon">🦙</span>
            <span class="tech-name">LLaMA</span>
        </div>
        <div class="tech-bubble" data-tech="gpt">
            <span class="tech-icon">AI</span>
            <span class="tech-name">GPT-4</span>
        </div>
        <div class="tech-bubble" data-tech="nocode">
            <span class="tech-icon">⚡</span>
            <span class="tech-name">NoCode</span>
        </div>
        <div class="tech-bubble" data-tech="finetune">
            <span class="tech-icon">🎯</span>
            <span class="tech-name">Fine-tune</span>
        </div>
        <div class="tech-bubble" data-tech="ml">
            <span class="tech-icon">🧠</span>
            <span class="tech-name">ML</span>
        </div>
    </div>

    <!-- Enhanced Live Activity Bar with Auto-Scroll -->
    <div class="activity-bar">
        <div class="activity-wrapper" id="activityWrapper">
            <span class="activity-item">
                <i class="fas fa-circle pulse"></i>
                <strong>Live:</strong> 342 students online
            </span>
            <span class="activity-item">
                <i class="fas fa-graduation-cap"></i>
                New course: Advanced Prompt Engineering
            </span>
            <span class="activity-item">
                <i class="fas fa-trophy"></i>
                Sarah completed AI Fundamentals
            </span>
            <span class="activity-item">
                <i class="fas fa-fire"></i>
                Trending: LLaMA Fine-tuning Workshop
            </span>
            <span class="activity-item">
                <i class="fas fa-star"></i>
                New achievement: 10K+ certificates issued
            </span>
            <span class="activity-item">
                <i class="fas fa-rocket"></i>
                Launch: Neural Networks Masterclass
            </span>
        </div>
    </div>

    <!-- Enhanced Statistics Bar -->
    <div class="stats-bar-compact">
        <div class="container">
            <div class="stats-grid-compact">
                <div class="stat-item-compact">
                    <i class="fas fa-users"></i>
                    <div>
                        <h3>15,000<span class="stat-suffix">+</span></h3>
                        <p>Active Learners</p>
                    </div>
                </div>
                <div class="stat-item-compact">
                    <i class="fas fa-book-open"></i>
                    <div>
                        <h3>250<span class="stat-suffix">+</span></h3>
                        <p>AI Courses</p>
                    </div>
                </div>
                <div class="stat-item-compact">
                    <i class="fas fa-certificate"></i>
                    <div>
                        <h3>8,500<span class="stat-suffix">+</span></h3>
                        <p>Certificates</p>
                    </div>
                </div>
                <div class="stat-item-compact">
                    <i class="fas fa-star"></i>
                    <div>
                        <h3>98<span class="stat-suffix">%</span></h3>
                        <p>Success Rate</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Footer Content -->
    <div class="footer-main-optimized">
        <div class="container">
            <div class="footer-grid-optimized">
                <!-- Brand & Social Column -->
                <div class="brand-column-opt">
                    <div class="brand-header">
                        <a href="{{{ config.wwwroot }}}/" class="footer-logo">
                            {{#main_footer_logo}}
                                <img src="{{main_footer_logo}}" alt="{{sitename}}">
                            {{/main_footer_logo}}
                            {{^main_footer_logo}}
                                <h2 class="text-logo-opt">
                                    <span>Bheem</span>
                                    <span class="accent">Academy</span>
                                </h2>
                            {{/main_footer_logo}}
                        </a>
                    </div>

                    <p class="brand-desc">
                        Revolutionizing AI education for all ages. From curious 8-year-olds to experienced professionals in their 80s.
                    </p>

                    <div class="social-links-opt">
                        <a href="https://www.facebook.com/bheemacademy" target="_blank" rel="noopener noreferrer" class="social-link-opt" data-network="facebook" title="Follow us on Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="https://twitter.com/bheemacademy" target="_blank" rel="noopener noreferrer" class="social-link-opt" data-network="twitter" title="Follow us on Twitter">
                            <i class="fab fa-twitter"></i>
                        </a>
                        <a href="https://www.linkedin.com/company/bheemacademy" target="_blank" rel="noopener noreferrer" class="social-link-opt" data-network="linkedin" title="Connect on LinkedIn">
                            <i class="fab fa-linkedin-in"></i>
                        </a>
                        <a href="https://www.instagram.com/bheemacademy" target="_blank" rel="noopener noreferrer" class="social-link-opt" data-network="instagram" title="Follow us on Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a href="https://www.youtube.com/@bheemacademy" target="_blank" rel="noopener noreferrer" class="social-link-opt" data-network="youtube" title="Subscribe on YouTube">
                            <i class="fab fa-youtube"></i>
                        </a>
                    </div>
                </div>

                <!-- Quick Links -->
                <div class="links-column-opt">
                    <h3 class="column-title">
                        <i class="fas fa-compass"></i>
                        Quick Links
                    </h3>
                    <div class="links-grid">
                        <a href="{{{ config.wwwroot }}}/course/" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> All Courses
                        </a>
                        <a href="{{{ config.wwwroot }}}/my/" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Dashboard
                        </a>
                        <a href="{{{ config.wwwroot }}}/badges/mybadges.php" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> My Certificates
                        </a>
                        <a href="{{{ config.wwwroot }}}/blog/" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Blog & News
                        </a>
                        <a href="{{{ config.wwwroot }}}/mod/forum/" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Community
                        </a>
                        <a href="{{{ config.wwwroot }}}/user/profile.php" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> My Profile
                        </a>
                    </div>
                </div>

                <!-- Resources -->
                <div class="links-column-opt">
                    <h3 class="column-title">
                        <i class="fas fa-book"></i>
                        Resources
                    </h3>
                    <div class="links-grid">
                        <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=3" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Help Center
                        </a>
                        <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=4" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Documentation
                        </a>
                        <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=5" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> AI Tools
                        </a>
                        <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=6" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> API Access
                        </a>
                        <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=7" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Downloads
                        </a>
                        <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=11" class="footer-link-opt">
                            <i class="fas fa-angle-right"></i> Contact Us
                        </a>
                    </div>
                </div>

                <!-- Newsletter & Contact -->
                <div class="newsletter-column-opt">
                    <h3 class="column-title">
                        <i class="fas fa-envelope"></i>
                        Stay Updated
                    </h3>

                    <div class="newsletter-box-opt">
                        <p class="newsletter-text">Get exclusive AI insights and course updates</p>
                        <form class="newsletter-form-opt" id="newsletterFormOpt">
                            <input type="email" placeholder="Enter your email" required>
                            <button type="submit">
                                <span>Subscribe</span>
                                <i class="fas fa-arrow-right"></i>
                            </button>
                        </form>
                        <div class="newsletter-features">
                            <span><i class="fas fa-check"></i> Weekly AI News</span>
                            <span><i class="fas fa-check"></i> Exclusive Offers</span>
                            <span><i class="fas fa-check"></i> Free Resources</span>
                        </div>
                    </div>

                    <div class="contact-info-opt">
                        <div class="contact-item-opt">
                            <i class="fas fa-headset"></i>
                            <span>24/7 Support Available</span>
                        </div>
                        <div class="contact-item-opt">
                            <i class="fas fa-envelope"></i>
                            <span>support@bheem.academy</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Featured Courses Slider -->
            <div class="featured-courses-bar">
                <h4><i class="fas fa-fire"></i> Trending Courses:</h4>
                <div class="courses-slider">
                    <a href="{{{ config.wwwroot }}}/course/" class="course-chip">
                        <i class="fas fa-robot"></i> Intro to ChatGPT
                    </a>
                    <a href="{{{ config.wwwroot }}}/course/" class="course-chip">
                        <i class="fas fa-brain"></i> Neural Networks 101
                    </a>
                    <a href="{{{ config.wwwroot }}}/course/" class="course-chip">
                        <i class="fas fa-code"></i> Python for AI
                    </a>
                    <a href="{{{ config.wwwroot }}}/course/" class="course-chip">
                        <i class="fas fa-palette"></i> AI Art Generation
                    </a>
                    <a href="{{{ config.wwwroot }}}/course/" class="course-chip">
                        <i class="fas fa-chart-line"></i> Data Science Basics
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- Footer Bottom -->
    <div class="footer-bottom-opt">
        <div class="container">
            <div class="bottom-content-opt">
                <div class="copyright-opt">
                    <p>© 2025 Bheem Academy • Empowering minds through AI</p>
                </div>
                <div class="legal-links-opt">
                    <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=8">Privacy Policy</a>
                    <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=9">Terms of Service</a>
                    <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=10">Cookie Policy</a>
                    <a href="{{{ config.wwwroot }}}/mod/page/view.php?id=12">Sitemap</a>
                </div>
                <div class="payment-badges">
                    <i class="fab fa-cc-visa"></i>
                    <i class="fab fa-cc-mastercard"></i>
                    <i class="fab fa-cc-paypal"></i>
                    <i class="fab fa-cc-stripe"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Floating Chat Widget -->
    <div class="chat-widget">
        <button class="chat-trigger">
            <i class="fas fa-comments"></i>
            <span class="chat-badge">1</span>
        </button>
    </div>
</footer>

<script>
// Newsletter Form Handler
document.getElementById('newsletterFormOpt')?.addEventListener('submit', function(e) {
    e.preventDefault();
    const email = this.querySelector('input[type="email"]').value;
    alert('Thank you for subscribing! We\'ll send AI updates to ' + email);
    this.reset();
});

// Auto-scroll Activity Bar - Duplicate content for seamless infinite scroll
document.addEventListener('DOMContentLoaded', function() {
    const activityWrapper = document.getElementById('activityWrapper');
    if (activityWrapper) {
        const items = activityWrapper.innerHTML;
        activityWrapper.innerHTML = items + items; // Duplicate for seamless loop
    }
});
</script>

{{{ output.standard_end_of_body_html }}}
